---
  - name: Ping Servers
    hosts: prod
    tasks:
      - name: Ping
        ping:
        when: "'prod' in groups"
  
  - name: Ping Ubuntu Server
    hosts: host3
    tasks:
      - name: Ping
        ping:
        when: ansible_distribution == "Ubuntu" and ansible_distribution_version == "18.04"